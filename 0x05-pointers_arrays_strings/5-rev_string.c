#include "main.h"

/**
 * rev_string - function that reverses a string
 * @s: string to reverse
 */

void rev_string(char *s)
{
	int i;
	int j;
	int n;

	for (i = 0; i < s[n]; i++)
	{
		for (j = (s[n] - 1); n != 0; j--)
		{
			_putchar(s[j]);
		}
	}
}
