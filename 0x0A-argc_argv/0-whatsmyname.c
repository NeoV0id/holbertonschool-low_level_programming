#include <stdio.h>

/**
 * main - prints its own executable name
 * @argc: arguments count
 * @argv: arguments vector
 *
 * Return: 0 for success
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
