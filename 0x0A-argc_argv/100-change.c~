#include <stdio.h>

/**
 * main - give the minimum number of coins to make change for an amount of money
 *
 *
 * Return: 0 for success and 1 for failure
 */

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		if ((argc < 2) && (argc > 2))
		{
			printf("Error\n");
			return (1);
		}

		else if (
	}
}
