#include "main.h"

/**
 * get_bit - returns the value of a bit at a given index
 * @n: number
 * @index: index of the bit to check
 *
 * Return: the value of the bit checked
 */

int get_bit(unsigned long int n, unsigned int index)
{
	unsigned int i;

	if (n == NULL || index == NULL)
		return (-1);

	for (i = 0; i < index; i++)
		
}
