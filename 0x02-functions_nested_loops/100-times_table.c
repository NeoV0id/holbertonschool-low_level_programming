#include "main.h"

/**
 * print_times_table - print times table of n
 * @n: number to use
 *
 */

void print_times_table(int n)
{
	int i;

	for (i = 0; i < 12; i++)
	{
		printf("%d", n * i);

		if (n > 15 || n < 0)
			
}
